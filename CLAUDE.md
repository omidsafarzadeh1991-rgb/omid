# CLAUDE.md — قوانین ثابت پروژه «سیستم نوبت‌دهی هوشمند کلینیک»

> این فایل قوانین دائمی این پروژه است. کلود (Claude Code) باید در **همهٔ** مراحل توسعه، بدون نیاز به یادآوری دوباره توسط کاربر، این قوانین را رعایت کند. اگر قانونی با درخواست لحظه‌ای کاربر تناقض داشت، کلود باید موضوع را قبل از اجرا با کاربر مطرح کند، نه اینکه ساکت نادیده بگیرد یا ساکت اجرا کند.

## ۰. زمینهٔ پروژه (برای اینکه کلود همیشه context را حفظ کند)

سیستمی تحت وب می‌سازیم برای **نوبت‌دهی کلینیک‌ها** با این ویژگی‌ها:

- **اولویت پلتفرم:** ابتدا وب/دسکتاپ (ویندوز) → سپس موبایل (وب ریسپانسیو / PWA).
- **معماری چند-مستأجری (Multi-tenant):** هر کلینیک یک حساب مستقل دارد؛ داده‌های کلینیک‌ها از هم کاملاً جدا هستند.
- **اتصال به پیام‌رسان‌ها:** هر کلینیک با **توکن اختصاصی خودش** به تلگرام، بله، واتس‌اپ و اینستاگرام وصل می‌شود (هر کلینیک توکن‌های خودش را وارد می‌کند).
- **بات نوبت‌دهی:** کاربر نهایی (بیمار) از طریق بات در تلگرام/بله/واتس‌اپ/اینستاگرام نوبت رزرو می‌کند؛ بات مثل منشی رفتار می‌کند (پاسخ‌گویی هوشمند + ثبت نوبت).
- **ثبت دستی:** منشی کلینیک باید بتواند از پنل وب هم برای تلگرام و هم برای بله، نوبت را **دستی ثبت کند**.
- **قانون طلایی سیستم:** هیچ‌وقت دو نفر نباید یک اسلات زمانی را همزمان بگیرند — فارغ از این‌که رزرو از کدام کانال (بات تلگرام، بات بله، ثبت دستی منشی، ...) آمده باشد. اگر ساعتی رزرو شده، بات/پنل باید فوراً بفهمد که پر است و اجازهٔ رزرو مجدد ندهد.
- کاربر پروژه (owner) برنامه‌نویس نیست؛ همه چیز باید **قدم‌به‌قدم، با دستورات کپی‌پیست‌شدنی** توضیح داده شود.
- **امنیت داده‌ها** و **ظاهر/UX حرفه‌ای** دو اولویت غیرقابل‌مذاکره پروژه هستند.

---

## ۱. نحوهٔ ارتباط با کاربر (غیرفنی است، باید ساده صحبت شود)

1. همیشه به **فارسی روان و غیرفنی** توضیح بده؛ اگر اصطلاح فنی لازم است، در پرانتز یک‌خطی معنی‌اش را بگو.
2. هر مرحله را به‌صورت **گام‌های شماره‌دار و کوچک** بده؛ هر گام باید یک کار مشخص و قابل تست باشد، نه یک پرش بزرگ.
3. دستورات نصب/اجرا را همیشه در بلوک کد جدا و **آماده کپی‌پیست** بده (بدون نیاز به ویرایش دستی توسط کاربر)، مگر جایی که کاربر باید مقدار خودش (مثل توکن) را جایگزین کند — آن‌جا را با `<اینجا توکن را بگذار>` مشخص کن.
4. قبل از هر اقدامی که **هزینه‌بر**، **برگشت‌ناپذیر**، یا **نیازمند تایید بیرونی (مثل تایید Meta Business برای واتس‌اپ/اینستاگرام)** است، حتماً اول توضیح بده و تاییدِ صریح کاربر را بگیر.
5. اگر تصمیمی معماری/استراتژیک است (انتخاب استک، هاستینگ، دیتابیس، ترتیب فازها) و چند گزینهٔ منطقی دارد، از کاربر بپرس (با گزینه‌های ساده و پیشنهاد پیش‌فرض)؛ خودسرانه تصمیم نگیر مگر بدیهی باشد.
6. در پایان هر مرحله، خلاصهٔ کوتاه بده: «چه چیزی ساخته شد»، «چطور تستش کنیم»، «قدم بعدی چیست».

---

## ۲. امنیت داده‌ها (خط قرمز پروژه)

1. **هیچ توکن، رمز، کلید API یا اطلاعات حساس هرگز در کد یا گیت commit نشود.** همیشه در متغیرهای محیطی (`.env`) که در `.gitignore` هستند نگه‌داری شود.
2. توکن‌های هر کلینیک (تلگرام، بله، واتس‌اپ، اینستاگرام) باید در دیتابیس **رمزنگاری‌شده (encrypted at rest)** ذخیره شوند، نه متن ساده.
3. جداسازی کامل داده بین کلینیک‌ها (tenant isolation) — هر query باید به `clinic_id` محدود شود؛ نباید هیچ مسیری وجود داشته باشد که کلینیک A به دادهٔ کلینیک B (نوبت‌ها، بیماران، توکن‌ها) دسترسی پیدا کند.
4. اطلاعات بیماران (نام، شماره تماس، تاریخچه نوبت) داده حساس محسوب می‌شود: رمز عبور‌ها هش‌شده (bcrypt/argon2)، دسترسی پنل با احراز هویت + نقش‌بندی (نقش منشی vs مدیر کلینیک vs سوپرادمین).
5. تمام endpointهای وبهوک بات‌ها (تلگرام/بله/واتس‌اپ/اینستاگرام) باید امضای درخواست (webhook secret/signature) را تایید کنند تا کسی نتواند جعل کند.
6. Rate limiting و validation ورودی روی همهٔ APIها؛ هیچ ورودی کاربر مستقیم به دیتابیس/شل نرود (جلوگیری از SQL injection و مشابه آن).
7. بک‌آپ منظم دیتابیس باید از همان فازهای اول طراحی شود، نه اضافه‌شدهٔ بعدی.
8. هر تغییر امنیتی مهم باید قبل از commit به کاربر توضیح داده شود (نه فقط انجام و رد شدن).

---

## ۳. قانون قفل‌شدن نوبت (Double-booking Prevention) — هستهٔ اصلی سیستم

1. منبع حقیقت (single source of truth) برای همهٔ نوبت‌ها **یک جدول واحد در دیتابیس مرکزی** است؛ فرقی نمی‌کند رزرو از بات تلگرام آمده، بات بله، یا ثبت دستی منشی — همه از همان جدول با هم رقابت می‌کنند.
2. رزرو یک اسلات باید به‌صورت **atomic/transactional** انجام شود (مثلاً با constraint یکتا روی `(clinic_id, doctor_id, slot_time)` در دیتابیس + transaction) تا در صورت رزرو همزمان از دو کانال مختلف، فقط یکی موفق شود.
3. قبل از نمایش اسلات‌های خالی به بات یا پنل دستی، همیشه از دیتابیس زنده خوانده شود؛ هیچ کش (cache) قدیمی نباید باعث نمایش اسلات پر به‌عنوان خالی شود.
4. اگر رزرو دوم روی اسلات پرشده انجام شود، سیستم باید با پیام فارسی مودبانه («این ساعت قبلاً رزرو شده، لطفاً یکی از این ساعت‌های خالی را انتخاب کنید: ...») آن را رد کند — نه خطای فنی خام.
5. **این بخش باید همیشه با تست‌های خودکار (concurrency tests) پوشش داده شود** — نمی‌توان صرفاً با چشم بررسی کرد که درست کار می‌کند.

---

## ۴. الزام تست در هر مرحله (Definition of Done)

هیچ قابلیتی «تمام‌شده» اعلام نمی‌شود مگر این‌که:

1. تست‌های خودکار (unit/integration) برای منطق اصلی آن نوشته و اجرا شده باشد و **پاس شده باشد** (خروجی تست به کاربر نشان داده شود، نه فقط ادعای شفاهی).
2. برای قابلیت‌های مربوط به نوبت‌دهی، حتماً یک تست شبیه‌سازی «رزرو همزمان از دو کانال روی یک اسلات» اجرا و نتیجه‌اش گزارش شود.
3. یک چک‌لیست تست دستی (manual test checklist) ساده و فارسی هم نوشته شود تا کاربر غیرفنی هم بتواند خودش با کلیک‌کردن در مرورگر تایید کند که کار می‌کند.
4. قبل از رفتن به فاز بعد، فاز فعلی باید توسط کاربر تایید شده باشد («تست کردم، درست کار می‌کند»)؛ کلود نباید فاز بعد را روی پایهٔ تست‌نشده بنا کند.
5. هرجا باگ پیدا شد، ریشهٔ آن اصلاح شود (نه دور زدن/skip کردن تست).

---

## ۵. طراحی و تجربهٔ کاربری (UI/UX)

1. رابط کاربری باید **فارسی، راست‌به‌چپ (RTL)**، تمیز، مدرن و حرفه‌ای باشد (نه یک فرم خام و بی‌ظاهر).
2. از همان ابتدا طراحی باید **واکنش‌گرا (responsive)** باشد، حتی اگر فاز اول روی دسکتاپ تست می‌شود، چون قرار است بعداً موبایل هم اضافه شود.
3. برای بخش‌های بصری/طراحی از ابزارهای طراحی مناسب (skillهای frontend-design / ui-styling) استفاده شود؛ ظاهر پیش‌فرض کتابخانه‌ها بدون سفارشی‌سازی کافی نیست.
4. پیام‌های بات‌ها (تلگرام/بله/...) باید لحن مودبانه، شبیه منشی واقعی مطب، و کاملاً فارسی باشند.

---

## ۶. معماری و توسعه

1. کدنویسی تمیز، بدون انتزاع (abstraction) اضافه برای «شاید بعداً لازم شود» — فقط چیزی که فاز فعلی نیاز دارد.
2. بدون کامنت اضافی در کد؛ فقط جایی‌که یک تصمیم غیربدیهی (مثل قفل concurrency) نیاز به توضیح کوتاه دارد.
3. مدیریت پیام‌رسان‌ها (تلگرام/بله/واتس‌اپ/اینستاگرام) باید پشت یک لایهٔ مشترک (adapter/interface) پیاده شود تا افزودن پلتفرم جدید، بازنویسی هستهٔ نوبت‌دهی را لازم نداشته باشد.
4. فازبندی پیشنهادی (قابل تغییر با تایید کاربر):
   - فاز ۱: هستهٔ نوبت‌دهی + پنل وب مدیریت کلینیک (بدون بات) — تست کامل قفل‌شدن نوبت.
   - فاز ۲: بات تلگرام (رایگان و ساده‌ترین برای شروع) + ثبت دستی تلگرام از پنل.
   - فاز ۳: بات بله + ثبت دستی بله از پنل.
   - فاز ۴: واتس‌اپ (نیازمند Meta Business API — هزینه/تاییدیه دارد، حتماً قبل از شروع با کاربر هماهنگ شود).
   - فاز ۵: اینستاگرام (مشابه واتس‌اپ، از طریق Meta).
   - فاز ۶: نسخهٔ موبایل/PWA.
5. تغییر ترتیب فازها فقط با تایید صریح کاربر.
6. **تصمیمات تاییدشده توسط کاربر (۲ آگوست ۲۰۲۶):**
   - **فازبندی:** همان ترتیب بالا (وب+پنل → تلگرام → بله → واتس‌اپ → اینستاگرام → موبایل) تایید شد.
   - **هاستینگ فعلی:** در فاز توسعه/تست، همه‌چیز روی سیستم محلی کاربر (ویندوز) اجرا و تست می‌شود؛ انتخاب هاست نهایی (سرور ایرانی/خارجی) بعداً و قبل از فاز production با کاربر مشخص می‌شود. **نکتهٔ فنی مهم:** تلگرام/بله برای ارسال پیام به بات نیاز به یک آدرس عمومی (public HTTPS webhook) دارند؛ روی سیستم محلی این با ابزار تونل (مثل ngrok) در حالت تست شبیه‌سازی می‌شود، نه با هاست واقعی.
   - **نحوهٔ پاسخ‌گویی بات:** کاملاً هوش مصنوعی (چت آزاد شبیه منشی واقعی، نه فقط منوی دکمه‌ای). این یعنی:
     - بات از مدل Claude (از طریق Anthropic API، با کلید API که کاربر باید تهیه و پرداخت هزینهٔ مصرفی آن را انجام دهد) برای مکالمهٔ آزاد استفاده می‌کند.
     - AI هرگز مستقیم و آزاد یک نوبت را «قول» یا ثبت نمی‌کند؛ باید همیشه از طریق یک ابزار مشخص (function/tool call) به هستهٔ نوبت‌دهی (بخش ۳) وصل شود که خودش atomic و ضدتداخل است. یعنی تصمیم نهاییِ «آیا این ساعت خالی است یا نه» همیشه با دیتابیس است، نه با حدس AI.
     - AI باید محدود به حوزهٔ کاری‌اش (نوبت‌دهی، پرسش‌های عمومی مطب مثل آدرس/ساعت کاری) باشد و **هرگز مشاورهٔ پزشکی/تشخیص ندهد**؛ در صورت خارج‌شدن از حوزه، باید مودبانه ارجاع به تماس با مطب بدهد.
     - چون هر پیام به AI هزینه دارد، باید prompt طوری طراحی شود که کوتاه، دقیق، و کم‌مصرف باشد و کاربر از هزینهٔ تقریبی مطلع شود.

---

## ۷. Git و مدیریت نسخه

1. توسعه فقط روی برنچ تعیین‌شده انجام شود؛ commitهای واضح و توصیفی (فارسی یا انگلیسی، مهم نیست، مهم شفافیت است).
2. هیچ push به production یا merge نهایی بدون تایید صریح کاربر.
3. اقدامات مخرب/برگشت‌ناپذیر گیت (force push، reset --hard و مشابه) فقط با تایید صریح.
4. secrets/.env هرگز commit نشود؛ همیشه در `.gitignore` بررسی شود که وجود دارد.

---

## ۸. وقتی کلود مطمئن نیست

اگر کلود در مورد یک تصمیمِ مهم (هزینه، معماری، جمع‌آوری داده حساس، اتصال به سرویس بیرونی) شک دارد، باید **قبل از عمل** از کاربر بپرسد؛ حدس زدن یا پیش‌فرض گرفتن ساکت در این موارد ممنوع است.
