// Vitest runs library code outside of Next.js's bundler, where the real
// "server-only" package throws unconditionally. This stub is aliased in
// vitest.config.ts so tests can still exercise server-only modules.
export {};
